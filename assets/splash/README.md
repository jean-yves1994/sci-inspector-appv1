Place `sci_logo.png` here (min 512x512, transparent background).

Then generate the native splash:

    dart run flutter_native_splash:create

Until the asset exists, the in-app splash renders a vector "SCI" placeholder.
